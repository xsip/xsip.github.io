import { RainyImageDirective } from './rainy-image.directive';

describe('RainyImageDirective', () => {
  it('should create an instance', () => {
    const directive = new RainyImageDirective();
    expect(directive).toBeTruthy();
  });
});
