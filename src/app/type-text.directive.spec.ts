import { TypeTextDirective } from './type-text.directive';

describe('TypeTextDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeTextDirective();
    expect(directive).toBeTruthy();
  });
});
